{
  "name": "autoreport-frontend",
  "version": "1.0.0",
  "type": "module",
  "description": "AutoReport — Serveur frontend SPA avec proxy API",
  "scripts": {
    "start": "node server.js",
    "pm2": "pm2 start ecosystem.config.cjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
